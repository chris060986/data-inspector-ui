import { DataTriggerDirective } from './data-trigger.directive';

describe('DataTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new DataTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
